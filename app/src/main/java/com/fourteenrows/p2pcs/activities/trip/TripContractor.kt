package com.fourteenrows.p2pcs.activities.trip

import com.fourteenrows.p2pcs.generalview.IView

interface TripContractor {
    interface View : IView

    interface Presenter

    interface Interactor

    interface CompleteListener
}